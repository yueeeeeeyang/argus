# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.87"
name = "rars-crypto"
version = "0.2.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "RAR legacy and modern archive encryption primitives."
homepage = "https://github.com/bitplane/rars"
documentation = "https://docs.rs/rars"
readme = false
keywords = [
    "rar",
    "archive",
    "compression",
]
categories = ["cryptography"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/bitplane/rars"

[package.metadata.docs.rs]
all-features = true

[features]
fast = ["rars-crc32/fast"]
parallel = ["rars-crc32/parallel"]

[lib]
name = "rars_crypto"
path = "src/lib.rs"

[dependencies.aes]
# Argus 的 SMB 与 russh 使用稳定版 aes 0.9；这里跟随同一版本以避免 Cargo 在同一个
# 0.9 系列中同时解析稳定版和预发布版导致依赖冲突。
version = "0.9.1"
features = ["zeroize"]

[dependencies.hmac]
version = "0.13"

[dependencies.rars-crc32]
version = "0.2.0"

[dependencies.sha1]
version = "0.11"

[dependencies.sha2]
version = "0.11"

[dependencies.zeroize]
version = "1.8"
features = [
    "alloc",
    "zeroize_derive",
]
default-features = false

[lints.clippy]
dbg_macro = "warn"
todo = "warn"
unimplemented = "warn"

[lints.rust]
unsafe_code = "forbid"
unused_must_use = "deny"
