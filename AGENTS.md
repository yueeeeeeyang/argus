# AGENTS.md

本文件适用于当前目录及其所有子目录，所有新增、修改、重构和审查工作必须遵守。规则分两级：红线违反即返工，其余为强制规范。

## 1. 红线

- 禁止在代码、注释、日志、配置中出现密码、令牌、密钥、身份证号、银行卡号、手机号等敏感信息；确需记录必须脱敏。
- 数据库结构、索引、约束、基础数据或数据修复变更一律通过新增迁移脚本实施；历史迁移脚本不可变——禁止修改、覆盖、重命名、删除或复用版本号，修正只能新增更高版本脚本前向修复。
- 禁止通过关闭检查规则、删除或弱化测试来让验证通过。
- 禁止静默失败与临时绕过：空异常捕获、忽略错误返回值、用默认成功掩盖失败、演示级实现、依赖人工兜底。

## 2. 工作方式

- 最小改动：只做目标要求的修改，不顺手重构、不清理无关代码、不留半成品。
- 修改前先阅读相关代码与既有约定，尊重当前架构和风格；如需偏离，说明原因、影响和迁移方案。
- 影响性能、兼容性、数据结构、接口契约或部署方式的改动，必须提供说明、迁移方案和回滚方案。
- 新增或修改行为必须配套测试，覆盖正常路径、关键边界和失败路径；修复缺陷必须添加可复现问题的回归测试。
- 提交前必须运行项目的格式化、静态检查、类型检查和测试并全部通过。相关命令未在项目中定义时，先查构建脚本和包管理清单等配置文件，仍不明确则向用户确认，禁止猜测；环境受限无法执行时，明确说明未验证项及风险。

## 3. 工程结构

- 按业务领域组织模块，职责单一、边界清晰、可独立测试；业务逻辑不得堆积在接口层、命令入口或数据访问层。
- 优先组合与依赖注入，避免全局可变状态、循环依赖、过度继承和隐藏副作用。
- 配置、密钥和环境差异外部化管理，禁止硬编码生产地址、凭据、环境开关和魔法数字。
- 同一业务规则只保留一个权威实现；公共能力合理抽象，不过度设计。
- 不保留向后兼容：过时代码直接删除，禁止添加兼容层。

## 4. 可靠性与安全

- 外部调用必须设置超时；重试必须有次数上限和退避策略；非幂等操作不得盲目重试。
- 覆盖输入校验、边界条件、空值、异常路径、并发安全和资源释放，保证幂等性与数据一致性。
- 数据库和批处理操作必须考虑事务边界、索引、分页、批量大小和锁竞争，避免 N+1 查询和无界加载；新迁移脚本必须具备唯一版本和语义化描述，并评估执行时长、锁表影响和回滚方案。
- 涉及并发、缓存、消息、定时任务或分布式状态时，明确一致性、重复消费、乱序、竞争和恢复策略。
- 对外部输入进行校验、规范化和必要编码，防止注入、越权和敏感信息泄露。

## 5. 注释与日志

- 统一使用中文。注释解释"为什么"而非复述"做了什么"；禁止无意义、临时或冗余注释；注释必须与代码同步更新。
- 接口与方法注释说明用途、参数、返回值和异常；复杂逻辑注释关键分支、状态变化、外部调用、重试、降级与失败原因。
- 日志级别正确：DEBUG 调试细节，INFO 关键业务节点，WARN 可恢复异常，ERROR 需要介入处理的失败；日志必须携带关键业务标识（如订单号、用户 ID），确保仅凭日志即可定位问题。
- 模型面向的提示词（系统提示词、工具描述、工具报错文案、进入模型上下文的占位与标注）使用英文；用户面向文本（界面、审批弹窗、日志、文档草稿）使用中文；注释与文档仍统一使用中文。

## 6. 命名

- 命名简单、准确、语义完整，直接表达业务含义：布尔用 `isEnabled`、`hasPermission` 式，方法用动宾结构，类和类型用名词，集合用复数，常量表达稳定含义。
- 反例：`data`、`info`、`obj`、`temp`、`util`、含糊缩写、拼音、无意义编号。同一概念全项目同名；重命名时同步更新代码、测试、日志、配置和文档。

## 7. 交付检查

完成任何代码任务前，逐项确认：

- 未触碰红线；无遗留过时代码与兼容层。
- 格式化、静态检查、类型检查和测试全部通过；受限未执行项已说明风险。
- 改动最小且配套测试到位；数据库变更仅通过新增迁移脚本实施。
- 注释与日志为中文、级别正确、含关键业务标识、无敏感信息。
